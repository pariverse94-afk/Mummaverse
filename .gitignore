# Local secrets for `wrangler pages dev`. NEVER commit this file.
.dev.vars
.dev.vars.*
!.dev.vars.example

# Cloudflare / build
.wrangler/
node_modules/

# OS noise
Thumbs.db
.DS_Store
desktop.ini

# local Claude Code tool config, not part of the site
.claude/
